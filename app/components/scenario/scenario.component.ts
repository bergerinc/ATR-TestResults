import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: "scenario",
    templateUrl:'scenario.component.html',
    inputs: ['feature']
})

export class ScenarioComponent{}