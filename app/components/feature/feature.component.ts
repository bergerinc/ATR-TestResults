import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: "feature",
    templateUrl: 'feature.component.html',
    inputs: ['run']
})

export class FeatureComponent {}